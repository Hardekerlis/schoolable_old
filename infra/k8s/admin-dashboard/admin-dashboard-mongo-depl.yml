apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-dashboard-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-dashboard-mongo
  template:
    metadata:
      labels:
        app: admin-dashboard-mongo
    spec:
      containers:
        - name: admin-dashboard-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: admin-dashboard-mongo-srv
spec:
  selector:
    app: admin-dashboard-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
