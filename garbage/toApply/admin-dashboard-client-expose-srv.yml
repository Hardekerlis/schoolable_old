apiVersion: v1
kind: Service
metadata:
  name: admin-dashboard-client-srv
spec:
  # externalIPs:
  #   - 240.170.212.76
  type: ClusterIP
  selector:
    app: admin-dashboard-client
  ports:
    - port: 443
      targetPort: 3000
      protocol: TCP
      name: https
