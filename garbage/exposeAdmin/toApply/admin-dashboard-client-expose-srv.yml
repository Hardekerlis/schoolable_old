apiVersion: v1
kind: Service
metadata:
  name: admin-dashboard-client-srv
spec:
  externalIPs:
    - 228.191.161.47
  # type: LoadBalancer
  selector:
    app: admin-dashboard-client
  ports:
    - port: 443
      targetPort: 3000
      protocol: TCP
      name: https
